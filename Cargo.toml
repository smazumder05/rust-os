[package]
name = "blog-os"
version = "0.1.0"
edition = "2026"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
bootloader = "0.9"

[build]
target = "x86_64-blog_os.json"
